"use client";

// AuthDebug component - DISABLED
// This component is disabled to prevent overlay issues in the admin panel.
// To re-enable for debugging, change the DISABLED constant to false.

export function AuthDebug() {
  // Component is disabled - always return null
  return null;
}
